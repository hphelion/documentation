<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic PUBLIC "-//OASIS//DTD DITA Topic//EN" "http://docs.oasis-open.org/dita/v1.1/OS/dtd/topic.dtd" >
<topic id="jow1429811350849" xml:lang="en-us">
<!-- Modification History
        
-->
    <title>Creating and Using Memory Profiles</title>
    <shortdesc>You can optionally save the memory allocations for a host as a
        profile, and apply the profile to other hosts.</shortdesc>
    <prolog><author/>
        <author/>
    </prolog>
    <body>
        
            <p>For more information about host memory allocation, see <xref href="jow1423087781001.xml"/>.</p>
            <p>As an alternative to the web administration interface, you can use the following CLI
                command to create a memory profile:</p>
            <codeblock>~(keystone-admin)$ system memprofile-add memoryprofile hostid</codeblock>
            <p>where memoryprofile is the name or UUID of the memory profile, and
                    hostid is the name or UUID of the host from which to create
                the profile.</p>
        
        <ol>
            <li conref="jow1426453628262.xml#jow1426453628262/step_N1002B_N10028_N1001C_N10001" id="step_N1002F_N1002C_N1001C_N10001" /> 
                
            
             
                <li>On the Memory tab, click <b>Create Memory Profile</b>.
                
                    <p>The <b>Create Memory Profile</b> dialog box appears.</p>
                    
                        <image href="jow1429812100638.image" id="image_bk3_byq_5r">
                            <alt>Create Memory Profile</alt>
                        </image>
                    
                
                </li>
             
                <li>Type a name for the memory profile.</li>
            
             
                <li>Click <b>Create Memory
                        Profile</b> to save the profile and close the dialog box.</li>
            
        </ol>
        
            <p>You can apply this profile to other hosts by editing the host
                settings in the web administration interface. For information about editing a host,
                see <xref href="jow1404333790046.xml"/>. You can also use the
                following CLI command to apply a memory profile to a host:</p>
            <codeblock>~(keystone-admin)$ system host-apply-memprofile hostid memoryprofile</codeblock>
            <p>where hostid is the name or UUID
                of the host, and memoryprofile is the name or
                UUID of the memory profile.</p>
            <p>To manage memory profiles, you can use the <b>Memory Profiles</b> tab
                on the <b>System Inventory</b> page, or you can use the following CLI
                commands:</p>
            <ul id="ul_x1x_plr_5r">
                <li>
                    <p>To list memory profiles:</p>
                    <codeblock>~(keystone-admin)$ system memprofile-list</codeblock>
                </li>
                <li>
                    <p>To show details for a memory profile:</p>
                    <codeblock>~(keystone-admin)$ system memprofile-show memoryprofile</codeblock>
                    <p>where memoryprofile is the name or UUID of the memory
                        profile.</p>
                </li>
                <li>
                    <p>To delete a memory profile:</p>
                    <codeblock>~(keystone-admin)$ system memprofile-delete memoryprofile</codeblock>
                    <p>where memoryprofile is the name or UUID of the memory
                        profile.</p>
                </li>
            </ul>
        
    </body>
</topic>