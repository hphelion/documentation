<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic PUBLIC "-//OASIS//DTD DITA Topic//EN" "topic.dtd">
<topic id="topic_x2x_vy4_lt">
  <title>EON Service</title>
  <body>
    <p>ESX integration in HP Helion OpenStack enables administrators to connect their cloud to an
      existing VMware vCenter installation. The VMware vCenter driver connects to ESXi clusters in a
      vCenter through Compute proxy nodes. The Nova scheduler sees the ESXi clusters as compute
      nodes and uses that information for scheduling a new instance.</p>
    <p>The ESX on boarding (EON) service is an inventory which interacts with the VMware vCenter
      server and collects the information available at the datacenters and clusters. This
      information is used for deployment and configuration of ESX Proxy Compute nodes. </p>
    <p>EON, a new service sub component, captures details of the vCenter server in a Cloud
      Controller and provides the details of the clusters for configuring the list of clusters
      managed by ESX Proxy Compute node. The ESX Proxy Compute driver communicates to VMware vCenter
      server through VI SDK.</p>
    <section>
      <title>ESX Host Commissioning</title>
      <p>ESX Host Commissioning is a functionality provided to a user to add additional ESX hosts to
        an already activated cluster. You can add ESX host to an already activated cluster if
        OVSvApp is installed in the host. With the OVSvApp installed, you can connect the tenant VMs
        which are being created on that host to the tenant network.</p>
      <p><!--Adding ESX host to an already activated cluster will require the host to have OVSvApp to be installed in order to connect the tenant VMs being  created on that host to the tenant network. --></p>
      <p>The following steps provides a detailed procedure on how to commission a new a host to an
        already activated cluster.</p>
      <p>
        <ol id="ol_ttd_bw4_lt">
          <li>List the current state of the cluster using EON python
              client.<codeblock># eon cluster-list --vcenter-id &lt;vCenter ID></codeblock><p>For
              example:</p><codeblock># eon cluster-list --vcenter-id BC9DED4E-1639-481D-B190-2B54A2BF5674
+------------+------------+-----------------+-----------------+
| MOID       | Name       | Datacenter      | Import Status   |
+------------+------------+-----------------+-----------------+
| domain-c21 | Cluster1   | DC1             | activated       |
+------------+------------+-----------------+-----------------+</codeblock></li>
          <li>Add host in the maintenance mode to the already activated cluster as shown in the
            following example.<p><image href="../media/esx/eon_service_host1.png"
                id="image_it1_3bp_lt"/></p></li>
          <li>Execute the following command to import the eon
              cluster.<codeblock><codeph># eon cluster-import --vcenter-id &lt;vCenter ID> --cluster-name &lt;Cluster Name> --cluster-moid &lt;Cluster Moid></codeph></codeblock><p>For
              example:</p><codeblock># eon cluster-import --vcenter-id BC9DED4E-1639-481D-B190-2B54A2BF5674 --cluster-name Cluster1 --cluster-moid domain-c21
+--------------+-----------+
| Property     | Value     |
+--------------+-----------+
| cpu_free     | 83071.73  |
| cpu_total    | 83072     |
| cpu_used     | 0.27      |
| datacenter   | DC1       |
| disk_free    | 1022.79   |
| disk_total   | 1023.75   |
| errors       | []        |
| memory_free  | 496.82    |
| memory_total | 511.76    |
| memory_used  | 14.94     |
| name         | Cluster1  |
| state        | importing |
| switches     | []        |
+--------------+-----------+</codeblock>After
            successful import the cluster state will change to partially_activated and the hosts
            would have moved to folder with ovsvapp installed.</li>
          <li>To know the cluster state, execute the following
              command:<codeblock># eon cluster-list --vcenter-id &lt;vCenter ID></codeblock><p>For
              example:</p><codeblock># eon cluster-list --vcenter-id BC9DED4E-1639-481D-B190-2B54A2BF5674
+------------+----------+------------+---------------------+
| MOID       | Name     | Datacenter | Import Status       |
+------------+----------+------------+---------------------+
| domain-c21 | Cluster1 | DC1        | partially_activated |
+------------+----------+------------+---------------------+</codeblock><p><image
                href="../media/esx/eon_service_host2.png" id="image_ush_nbp_lt"/></p></li>
          <li>Run cluster activate
              command.<codeblock><codeph># eon cluster-activate --vcenter-id &lt;vCenter ID> --cluster-moid &lt;Cluster Moid> </codeph></codeblock><p>For
              example:</p><codeblock>eon cluster-activate --vcenter-id BC9DED4E-1639-481D-B190-2B54A2BF5674 --cluster-moid domain-c22</codeblock><p>After
              successful activation the cluster state will change to activated state.</p></li>
          <li>To know whether the cluster is activate or not, execute the following
              command:<codeblock># eon cluster-list --vcenter-id &lt;vCenter ID></codeblock><p>For
              example:</p><codeblock># eon cluster-list --vcenter-id BC9DED4E-1639-481D-B190-2B54A2BF5674
+------------+----------+------------+---------------------+
| MOID       | Name     | Datacenter | Import Status       |
+------------+----------+------------+---------------------+
| domain-c21 | Cluster1 | DC1        | activated           |
+------------+----------+------------+---------------------+</codeblock></li>
          <li>Commit your cloud deploy configuration to the<xref
              href="using_git.dita#topic_u3v_1yz_ct"> local git repo</xref>, as follows:
            <codeblock>cd ~/helion/hos/ansible
git add -A
git commit -m "My config or other commit message"</codeblock></li>
          <li>Prepare your environment for deployment:
            <codeblock>ansible-playbook -i hosts/localhost config-processor-run.yml;
ansible-playbook -i hosts/localhost ready-deployment.yml;
cd /home/stack/scratch/ansible/next/hos/ansible;</codeblock></li>
          <li>Run the following
            playbook:<codeblock>ansible-playbook -i hosts/verb_hosts guard-deployment.yml
ansible-playbook -i hosts/verb_hosts osconfig-run.yml --limit '*esx-ovsvapp'
ansible-playbook -i hosts/verb_hosts hlm-deploy.yml --limit NEU-OVSVAPP</codeblock></li>
          <li>After successful deployment of the <b>hlm-deploy</b>, execute the following command
            from the deployer
              node.<codeblock># eon move-hosts –vcenter-id &lt;vCenter ID> --host &lt;host IP> –-status SUCCESS</codeblock><p>The
              host will be moved back to the original cluster as shown below:</p><p><image
                href="../media/esx/eon_service_host3.png" id="image_xhw_pbp_lt"/></p><p>
              <note type="tip">You can get the host IP address from
                  <codeph>pass_through.yml</codeph> file at
                  <codeph>/home/stack/helion/definition/data/</codeph> as shown below. <p><image
                    href="../media/esx/host_ip_add.png" id="image_dm5_4np_lt"/></p><p/></note>
            </p><p/></li>
        </ol>
      </p>
    </section>
  </body>
</topic>
